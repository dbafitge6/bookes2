<div class="container px-5 px-sm-0">
  
  <% if @book.errors.any? %>
  <%= @book.errors.count %>件のエラーが発生しました
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
  
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= image_tag @user.get_profile_image,size:'100x100' %> 
        <table class="table">
             <tbody>
              <tr></tr>
              <tr>
            	  <th>name</th>
            	  <th><%=@user.name %></th>
              </tr>
              <tr>
            	  <th>introduction</th>
            	  <th><%=@user.introduction %></th>
              </tr>
            </tbody>
        </table>
<div class="row">
    <%= link_to  edit_user_path(@user.id) ,class: "btn btn-outline-secondary btn-block" do %>
     <i class="fas fa-user-cog"></i> 
    <% end %>
    
</a></div>
      <h2 class="mt-3">New book</h2>
      <%= form_with model: @book do |f| %>
  <div class="form-group">
    <h4>title</h4>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <h4>opinion</h4>
    <%= f.text_area :body %>
  </div>
  <div class="form-group">
    <%= f.submit 'Create Book' %>
    <% end%>
  </div>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    
<% @books.each do |books| %>
  <tr>
  <td><%= link_to user_path(books.user) do %>
  <%= image_tag books.user.get_profile_image,size:'100x100' %>
  <% end %>
  </td>
  
   <td><%= link_to book_path(books) do %>      <!-- book_path(book)-->
  <%= books.title %>                           <!-- リンク(id)-->                          
  <% end %>
  </td>
  <td><%=books.body %></td>
  </tr>
<% end %>
  </tbody>
</table>
    </div>
  </div>
</div>